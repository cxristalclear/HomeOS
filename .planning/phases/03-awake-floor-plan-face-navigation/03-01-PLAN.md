---
phase: 03-awake-floor-plan-face-navigation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/engine/wakeFloor.ts
  - src/lib/engine/wakeFloor.test.ts
  - src/lib/engine/roomPeek.ts
  - src/lib/engine/roomPeek.test.ts
autonomous: true
requirements: [WAWK-04]
must_haves:
  truths:
    - "wakeFloor returns the Floor id holding the Next Thing when the Next Thing is placed in a Room"
    - "wakeFloor falls back to the Floor with the most due-today rooms when the Next Thing is an Errand or nothing is due"
    - "wakeFloor ties break to the lowest level; nothing-due anywhere returns the lowest-level Floor"
    - "roomPeek returns the worst-first due task name + owner for a RoomView, or null when the room has nothing due"
  artifacts:
    - "src/lib/engine/wakeFloor.ts"
    - "src/lib/engine/wakeFloor.test.ts"
    - "src/lib/engine/roomPeek.ts"
    - "src/lib/engine/roomPeek.test.ts"
  key_links:
    - "wakeFloor reuses nextThing(tasks, now) and buildLayoutView(tasks, layout, now) — it does NOT recompute due/attention"
---

<objective>
Add the one piece of real logic Phase 3 introduces: `wakeFloor(tasks, layout, now)` — the pure engine function (WAWK-04) that decides which Floor the wall opens to on wake — plus a small `roomPeek` derivation the awake tiles will consume for their one-line task preview. Both are pure, colocated-tested engine functions.

Purpose: The awake face (Plan 02) must open on the Floor holding the Next Thing, with a deterministic fallback. Per CLAUDE.md and wall-ui.md, this selection logic is the only wall logic that gets a unit test; the UI glue around it does not. Computing it as a pure helper keeps it out of components.
Output: `wakeFloor.ts` + `wakeFloor.test.ts`, `roomPeek.ts` + `roomPeek.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@src/lib/engine/layout.ts
@src/lib/engine/nextThing.ts
@src/lib/engine/buckets.ts
@src/lib/domain/types.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: wakeFloor pure engine function (WAWK-04)</name>
  <files>src/lib/engine/wakeFloor.ts, src/lib/engine/wakeFloor.test.ts</files>
  <read_first>
    - src/lib/engine/nextThing.ts — `nextThing(tasks, now): BucketItem | null` returns the worst-first item; `BucketItem.task` is the underlying Task with `room_id`.
    - src/lib/engine/layout.ts — `buildLayoutView(tasks, layout, now): LayoutView`; FloorView has `floor.level`, `rooms[].room.id`, `rooms[].room.floor_id`, per-floor `dueCount`, per-room `dueCount`/`needsAttention`. Floors come back sorted by `level` ascending. `isErrand(task)` returns true when `room_id == null`.
    - src/lib/domain/types.ts — `Floor { id, name, level }`, `Room { id, floor_id, slot }`, `Task` extends `TaskRow` (has `room_id`).
    - src/lib/engine/wakeFloor.test.ts patterns: model on buckets.test.ts / due.test.ts (vitest, node env, behavioral, build Task/Floor/Room fixtures inline).
  </read_first>
  <behavior>
    Signature: `wakeFloor(tasks: Task[], layout: { floors: Floor[]; rooms: Room[] }, now: number): string | null`. Returns a Floor id, or null only when there are zero configured floors.
    - Test: Next Thing is a simple task placed in a Room on Floor B (level 1) → returns Floor B's id (even if Floor A level 0 has more due rooms). The Next Thing wins outright when it is placed.
    - Test: Next Thing is an Errand (room_id null) → fallback: the Floor with the most rooms whose needsAttention is true. Build floors so one clearly has more attention rooms; assert that floor's id.
    - Test: Next Thing's room_id points at a Room not present in the layout (deleted room → treated as Errand by buildLayoutView) → same Errand fallback path.
    - Test: nothing due anywhere (nextThing returns null, all floors dueCount 0) → returns the lowest-level Floor's id.
    - Test: fallback tie — two floors each have the same count of attention rooms → returns the lower `level` floor's id.
    - Test: zero floors configured → returns null.
  </behavior>
  <action>Implement `wakeFloor` in src/lib/engine/wakeFloor.ts as a pure function (WAWK-04, per D in 03-CONTEXT.md "Wake behavior"). Algorithm: call `nextThing(tasks, now)`; if it returns a non-null item whose `task.room_id` is non-null AND that room_id exists in `layout.rooms`, look up the room's `floor_id` and return it. Otherwise (Errand Next Thing, unknown room, or nothing due) take the fallback: call `buildLayoutView(tasks, layout, now)`, score each FloorView by the COUNT of rooms with `needsAttention === true`, and return the id of the highest-scoring floor; break ties by lowest `floor.level`; when every floor scores zero (nothing due), return the lowest-level floor's id. Reuse `nextThing` and `buildLayoutView` — do NOT recompute due/attention or re-implement bucketing. Return null only when `layout.floors` is empty. Floors from buildLayoutView are already sorted by level ascending, so the first floor in that array is the lowest level — rely on that ordering for the tie-break and the nothing-due case rather than re-sorting. Document the no-debt note in a header comment: the fallback counts rooms that need attention today, never a debt or miss count.</action>
  <verify>
    <automated>npx vitest run src/lib/engine/wakeFloor.test.ts</automated>
  </verify>
  <done>All wakeFloor.test.ts cases pass: placed-Next-Thing→its floor, Errand-Next-Thing→attention fallback, unknown-room→fallback, nothing-due→lowest level, fallback tie→lowest level, zero floors→null. `npm run typecheck` and `npm run lint` clean for the new files.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: roomPeek worst-first preview helper</name>
  <files>src/lib/engine/roomPeek.ts, src/lib/engine/roomPeek.test.ts</files>
  <read_first>
    - src/lib/engine/layout.ts — `RoomView { room, tasks, dueCount, needsAttention }` and `ErrandView { tasks, dueCount, needsAttention }`; `isDueToday(task, now)`.
    - src/lib/engine/due.ts — `dueSince(task, now): number | null` (the timestamp a task became due; smaller/older = more urgent). Already imported by layout.ts.
    - src/lib/engine/buckets.ts — how chains surface an active step owner via `activeStep`; for the peek we only need the task name + the simple owner.
    - src/lib/domain/types.ts — `Owner = "me" | "her" | "anyone"`.
  </read_first>
  <behavior>
    Signature: `roomPeek(view: { tasks: Task[] }, now: number): { text: string; owner: Owner | null } | null`. Picks the worst-first DUE-TODAY task and returns its name + owner; returns null when no task in the view is due today.
    - Test: a room with two due-today tasks of different `dueSince` → returns the older (more overdue) one's name.
    - Test: a room with tasks none of which are due today → returns null.
    - Test: a room whose only due task is a chain → returns the chain task's name and the active step's owner.
    - Test: owner passthrough — a simple due task owned "me" → returns `{ text, owner: "me" }`.
  </behavior>
  <action>Implement `roomPeek` in src/lib/engine/roomPeek.ts as a pure derivation (Implementation Note 4 in 03-UI-SPEC.md). Filter `view.tasks` to those with `isDueToday(task, now)`; if none, return null. Sort the due tasks worst-first by `dueSince(task, now)` ascending (older/null-safe — guard null `dueSince` as 0 so a brand-new due task sorts first, matching the engine's "0 = new" convention); take the first. Return `{ text: task.name, owner }` where `owner` is the active chain step's owner for a chain (reuse `activeStep` from chain.ts) or the task's `owner` for a simple task. Do NOT add a new repository method or recompute attention — this is a render-time derivation over the RoomView/ErrandView the awake face already has. Keep it shape-agnostic: accept any object exposing `tasks: Task[]` so both RoomView and ErrandView can pass through.</action>
  <verify>
    <automated>npx vitest run src/lib/engine/roomPeek.test.ts</automated>
  </verify>
  <done>roomPeek.test.ts cases pass: worst-first selection, none-due→null, chain owner, owner passthrough. typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure functions over in-memory task/layout data already loaded by the wall. No I/O, no new input, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | wakeFloor / roomPeek inputs | low | accept | Functions are pure and total; malformed rows already normalized upstream by buildLayoutView (unknown room → Errand). No new trust boundary; no mitigation required. |
| T-03-SC | Tampering | npm installs | low | accept | No new dependencies added in this plan (engine-only). No package-manager activity to gate. |
</threat_model>

<verification>
- `npx vitest run src/lib/engine/wakeFloor.test.ts src/lib/engine/roomPeek.test.ts` — all green.
- `npm run typecheck` clean.
- `npm run lint` clean (CI runs --max-warnings=0).
- `npm test` (full suite) still green — no regressions in existing engine tests.
</verification>

<success_criteria>
- `wakeFloor` selects the Next Thing's Floor; falls back to most-attention floor (tie → lowest level) for Errand/nothing-due, returning lowest level when nothing is due (WAWK-04).
- `roomPeek` yields the worst-first due preview + owner, or null.
- Both reuse the existing engine (`nextThing`, `buildLayoutView`, `dueSince`, `activeStep`) — no recomputation, no new debt/miss counts.
</success_criteria>

<output>
Create `.planning/phases/03-awake-floor-plan-face-navigation/03-01-SUMMARY.md` when done.
</output>
