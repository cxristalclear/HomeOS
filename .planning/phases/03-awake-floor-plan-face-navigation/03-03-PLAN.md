---
phase: 03-awake-floor-plan-face-navigation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/app/wall/constants.ts
  - src/app/wall/FloorIndicator.tsx
  - src/app/wall/AwakeLayer.tsx
  - src/app/wall/page.tsx
autonomous: true
requirements: [WNAV-02, WNAV-03]
user_setup: []
must_haves:
  truths:
    - "The ambient↔awake switch crossfades/scales over ~400ms, snapping instantly under prefers-reduced-motion (WNAV-01 polish)"
    - "Swiping horizontally on the awake face changes the displayed Floor; all configured floors are reachable; swipe clamps at the ends (WNAV-03)"
    - "A floor indicator shows the current Floor by name and lets the user tap to switch floors (WNAV-03)"
    - "After ~90s with no interaction the wall returns to ambient; any tap or swipe resets the timer (WNAV-02)"
    - "The Errands tile stays pinned regardless of which Floor is shown (WAWK-02 across floors)"
  artifacts:
    - "src/app/wall/constants.ts"
    - "src/app/wall/FloorIndicator.tsx"
  key_links:
    - "page.tsx owns activeFloorId + the idle timer (reusing the visibilitychange pattern) and the face crossfade state; AwakeLayer hosts the touch handlers + FloorIndicator and reports floor changes / interactions up"
---

<objective>
Layer the navigation and lifecycle polish onto the awake face from Plan 02: the ~400ms ambient↔awake crossfade (reduced-motion aware), horizontal swipe between all floors with a tappable FloorIndicator, and the ~90s idle-return to ambient. After this slice the wall is the full Phase 3 experience — wake, browse every floor, return to calm on its own.

Purpose: Completes WNAV-02 (idle return) and WNAV-03 (swipe + floor indicator + Errands pinned across floors), and finishes the WNAV-01 transition with its specified animation. Selection and tile rendering already work from Plan 02.
Output: a shared `constants.ts` (the named idle timeout), the FloorIndicator component, AwakeLayer extended with touch swipe + indicator + crossfade styling, and page.tsx extended with floor switching + the idle timer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/03-awake-floor-plan-face-navigation/03-UI-SPEC.md
@.planning/phases/03-awake-floor-plan-face-navigation/03-CONTEXT.md
@src/app/wall/page.tsx
@src/app/wall/AwakeLayer.tsx
@.planning/phases/03-awake-floor-plan-face-navigation/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wall constants + FloorIndicator</name>
  <files>src/app/wall/constants.ts, src/app/wall/FloorIndicator.tsx</files>
  <read_first>
    - .planning/phases/03-awake-floor-plan-face-navigation/03-UI-SPEC.md — Component Inventory 8 (FloorIndicator + FloorIndicatorProps), Interaction Contract "Idle return" (the named `IDLE_TIMEOUT_MS = 90_000` constant), and "Floor tap (FloorIndicator)" (tapping a floor name selects it directly).
    - src/lib/engine/layout.ts — `FloorView` (has `floor.id`, `floor.name`, `floor.level`); floors are level-ordered.
    - src/app/wall/WallQueue.tsx — established wall class idioms (font-wall-sans, wall-hairline, wall-acc, text-faint/ink).
  </read_first>
  <action>Create src/app/wall/constants.ts exporting `IDLE_TIMEOUT_MS = 90_000` (per WNAV-02 and Implementation Note 6; the named, tunable ~90s constant). Add a short doc comment noting it is the awake→ambient idle return window.

  Create FloorIndicator.tsx implementing `FloorIndicatorProps` (floors: FloorView[], activeFloorId, onSelectFloor). Render the compact horizontal rail per the UI-SPEC: a flex row, one button per floor showing `floor.name` (no "Floor N" prefix), the active floor styled `text-ink bg-surface wall-hairline` with a preceding teal `wall-acc` 4px dot, inactive floors `text-faint` with no background. Use the exact class strings from the UI-SPEC section 8. Each button: role="button", aria-pressed={isActive}, aria-label="Floor: {floor.name}", onPointerDown → onSelectFloor(floor.id). Names, not bare dots (per 03-CONTEXT.md).</action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>constants.ts exports IDLE_TIMEOUT_MS = 90_000. FloorIndicator renders floor names with the active floor highlighted (teal dot + surface bg) and fires onSelectFloor on tap. typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: AwakeLayer — swipe handlers, FloorIndicator, crossfade styling</name>
  <files>src/app/wall/AwakeLayer.tsx</files>
  <read_first>
    - src/app/wall/AwakeLayer.tsx — the component from Plan 02 (cap + grid; currently shows a single FloorView).
    - .planning/phases/03-awake-floor-plan-face-navigation/03-UI-SPEC.md — Component Inventory 1 (AwakeLayer crossfade classes: opacity/transform transition, duration-[400ms], reduced-motion → transition none), Interaction Contract "Floor Swipe Navigation" (native touchstart/move/end, 40px threshold, swipe left→higher level / right→lower level, clamp at ends, no external lib; the 280ms translate animation; Errands NOT part of the swipe deck), and Implementation Note 7 (useRef for touchStartX, no useState for touch tracking).
    - src/app/wall/FloorIndicator.tsx — the component from Task 1.
  </read_first>
  <action>Extend AwakeLayer. Add new props so the page can drive multi-floor navigation: `floors: FloorView[]` (all floors, level-ordered), `activeFloorId: string`, `onSelectFloor(floorId)`, `onInteraction()` (called on any tap/swipe so the page can reset the idle timer), plus a `face`/`visible` flag so the layer can render its crossfade in/out state. Keep rendering the active FloorView (find it in `floors` by activeFloorId) via the existing cap + grid. Add native touch handlers (onTouchStart/onTouchMove/onTouchEnd) on the layer using a `useRef` for touchStartX (Implementation Note 7 — no useState for touch tracking): on touchend, if horizontal displacement exceeds the 40px threshold, compute the next floor by level order — swipe left → next-higher level, swipe right → next-lower level — and call onSelectFloor with the neighbor's id, clamping at the ends (no wrap). Call onInteraction() on touchstart/pointerdown so the idle timer resets on any interaction. Render the FloorIndicator at the bottom (mt-auto), passing floors/activeFloorId/onSelectFloor. Apply the AwakeLayer crossfade classes from the UI-SPEC driven by the visible/face prop (opacity 0→1, scale 0.985→1, transition-[opacity,transform] duration-[400ms] ease-in-out, pointer-events none→auto); under prefers-reduced-motion set the transition to none. The Errands tile must stay pinned and NOT translate with the floor deck (per UI-SPEC) — keep it rendered alongside the grid as it was in Plan 02; the floor-change re-render swaps the room tiles only. Keep attention/peek sourced from engine output — do not recompute.</action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>AwakeLayer switches the rendered FloorView on swipe (40px threshold, clamped at ends) and on FloorIndicator tap, calls onInteraction on every interaction, and carries the 400ms crossfade styling (reduced-motion → no animation). Errands tile stays pinned across floors. typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: page.tsx — floor switching, crossfade state, 90s idle timer (WNAV-02)</name>
  <files>src/app/wall/page.tsx</files>
  <read_first>
    - src/app/wall/page.tsx — from Plan 02: `face`, `activeFloorId`, `selectedRoomId`, `layoutView`, the onWake handler, the existing midnight + visibilitychange timer effect (the pattern to reuse for the idle timer).
    - src/app/wall/constants.ts — `IDLE_TIMEOUT_MS`.
    - src/app/wall/AwakeLayer.tsx — the extended props from Task 2 (floors, activeFloorId, onSelectFloor, onInteraction, visible/face).
    - .planning/phases/03-awake-floor-plan-face-navigation/03-UI-SPEC.md — Interaction Contract "Awake → Ambient Transition" (idle expiry → ambient; timer reset on any awake-face pointerdown; clear on visibility hide, restart fresh on show) and the ambient layer inverse crossfade (opacity 1→0, scale 1→1.03).
  </read_first>
  <action>Extend page.tsx. (1) Floor switching: add `onSelectFloor(floorId)` to set activeFloorId, and pass `floors` (layoutView.floors), activeFloorId, onSelectFloor, and onInteraction down to AwakeLayer. (2) Crossfade: drive both layers from the existing `face` state — apply the ambient-layer inverse crossfade classes (opacity 1→0, scale 1→1.03 when awake) from the UI-SPEC to the ambient content wrapper, and pass the visible/face flag to AwakeLayer so its crossfade-in fires; keep both layers mounted and absolutely positioned within the wakeable container so they overlay during the transition. Honor prefers-reduced-motion by letting the CSS media query in the class strings zero the duration (no JS branch needed if the classes already gate it; otherwise gate the duration class on a matchMedia check). (3) Idle timer (WNAV-02): add a `useRef` timer handle and a `resetIdle()` that clears and restarts a `setTimeout(IDLE_TIMEOUT_MS)` whose expiry sets face back to "ambient" and clears selectedRoomId. Start the timer on wake; reset it from AwakeLayer's onInteraction callback. Reuse the existing visibilitychange pattern: on visibility hide clear the idle timer; on show, if awake, restart it fresh (do not fire immediately). Ensure the idle effect only runs while face === "awake" and is cleaned up on unmount / when returning to ambient. Do NOT change WallTopBar/WallFooter or the phone surfaces.</action>
  <verify>
    <automated>npm run build && npm run typecheck && npm run lint</automated>
  </verify>
  <done>`npm run build` succeeds. Wake crossfades to awake (~400ms; instant under reduced-motion); swipe + floor indicator switch floors across all configured floors with Errands pinned; ~90s of no interaction returns to ambient and any tap/swipe resets the timer; visibility hide/show clears/restarts the timer cleanly. typecheck + lint clean; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch/tap → client state | swipe/tap/idle events drive `face`, `activeFloorId`, selection, and a setTimeout in client state only — no network write, no new API route, no auth (none by design). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Denial of Service | idle setTimeout / visibilitychange lifecycle | low | mitigate | Timer handle held in a ref and cleared on unmount, on return-to-ambient, and on visibility hide; restarted fresh on show — prevents leaked/duplicate timers on an always-on wall over long uptime. |
| T-03-05 | Information Disclosure | floors reachable via swipe | low | accept | All floors show the same shared household data already exposed; reaching another floor discloses nothing new (shared two-person display, no auth by design). |
| T-03-SC | Tampering | npm installs | medium | accept | No new npm dependencies — swipe is native touch handlers (no gesture library), per CONTEXT.md/CLAUDE.md. No package-manager activity to gate. |
</threat_model>

<verification>
- `npm run build` succeeds (CI gate).
- `npm run typecheck` clean; `npm run lint` clean (--max-warnings=0).
- `npm test` full suite green (no regressions).
- Manual (UI glue, intentionally not unit-tested per wall-ui.md): wake crossfades in (~400ms; reduced-motion snaps); swipe left/right and tap the floor indicator move through all floors and clamp at the ends; Errands stays pinned on every floor; leaving it ~90s returns to ambient; any tap/swipe resets the 90s window; backgrounding/foregrounding the tab does not fire an immediate return.
</verification>

<success_criteria>
- WNAV-02: ~90s idle returns the wall to ambient; any interaction resets the timer (named IDLE_TIMEOUT_MS constant; reuses the visibilitychange pattern).
- WNAV-03: swipe changes floors, all floors reachable, current floor indicated (FloorIndicator), Errands pinned across floors, clamp at ends.
- WNAV-01 transition polish: ~400ms crossfade/scale, reduced-motion aware.
- No phone-surface or skeleton (top bar/footer) changes.
</success_criteria>

<output>
Create `.planning/phases/03-awake-floor-plan-face-navigation/03-03-SUMMARY.md` when done.
</output>
